# Assets

Put a screenshot of the n8n workflow canvas here named **`workflow.png`** so it renders in the main README.

Tip: in n8n, add sticky notes to describe each step, click **fit-to-view**, then capture with **Win + Shift + S**. Make sure no credentials or tokens are visible before committing.
